plugins {
    id 'java'
}

dependencies {
    // Add specific dependencies for this subproject
    // Common dependencies are inherited from the root project
}
springBoot {
    mainClass.set('com.ap.RestaurantDataApplication')  // Replace with the fully qualified name of your main class
}
